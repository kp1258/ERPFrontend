import React from "react";
import { CloseOutlined } from "@ant-design/icons";
const CancelIcon = () => {
  return <CloseOutlined style={{ fontSize: "20px", color: "white" }} />;
};

export default CancelIcon;
