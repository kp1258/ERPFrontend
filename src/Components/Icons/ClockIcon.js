import React from "react";
import { ClockCircleOutlined } from "@ant-design/icons";
const ClockIcon = () => {
  return (
    <div style={{ height: "20px" }}>
      <div style={{ marign: "auto" }}>
        <ClockCircleOutlined style={{ fontSize: "15px", color: "blue" }} />
      </div>
    </div>
  );
};

export default ClockIcon;
